version: '3'
services:
  pbx-viewer:
    build: .
    ports:
      - "8501:8501"  # Change to Streamlit's default port
    env_file:
      - .env
    volumes:
      - .:/app
    restart: unless-stopped